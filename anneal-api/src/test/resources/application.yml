anneal:
  llm:
    enrichment-enabled: false

quarkus:
  datasource:
    devservices:
      enabled: false
    jdbc:
      url: jdbc:postgresql://localhost:5432/anneal
    username: admin
    password: admin